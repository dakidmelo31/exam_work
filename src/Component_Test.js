import React from 'react'
import Student from './Student'

export default function ComponentTest({students,togglePresence}) {
    //<div>there are {students.length} students </div> 
    return (
        
         <div></div>     
    )
}
